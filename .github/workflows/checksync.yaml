# THIS FILE IS AUTOGENERATED BY SYNCER DO NOT EDIT
name: Test for sync changes

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch: {}

jobs:
  check:
    name: Check for changesif: github.event_name != 'pull_request' || github.actor != 'cep21'
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v4
      - name: Check for changes
        uses: getsyncer/action-syncer/checkpr@main
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

  fix:
    name: Fix any sync changes
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' && github.actor == 'cep21'
    steps:
      - name: Check out code (with token)
        uses: actions/checkout@v4
        with:
          ssh-key: ${{ secrets.DEPLOY_KEY }}
      - name: Push back any changes
        uses: getsyncer/action-syncer/updatepr@main
        with:
          token: ${{ steps.generate_token.outputs.token }}
